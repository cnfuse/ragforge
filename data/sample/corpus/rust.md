# Rust

Rust is a systems programming language focused on safety, speed, and
concurrency. It guarantees memory safety without a garbage collector through its
ownership model and borrow checker, which enforce strict rules at compile time.

Originally sponsored by Mozilla, Rust is widely used for performance-critical
software such as operating systems, game engines, browsers, and embedded
systems. Its package manager and build tool is called Cargo.
